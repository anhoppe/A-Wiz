<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Awiz.View.UseCasePanelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Awiz"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Button Grid.Row="0" HorizontalAlignment="Stretch" Command="{Binding AddUseCaseCommand}">Add Use Case</Button>
        <Button Grid.Row="1" HorizontalAlignment="Stretch" Command="{Binding AddActorCommand}">Add Actor</Button>
        <Button Grid.Row="2" HorizontalAlignment="Stretch" Command="{Binding AddBoundaryCommand}">Add Boundary</Button>
    </Grid>
</UserControl>
